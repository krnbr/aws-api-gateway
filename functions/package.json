{
    "name": "functions",
    "main": "index.ts",
    "scripts": {
        "ping:build": "cd ping && npm i --production && cd .. && tsc ping/*.ts"
    },
    "devDependencies": {
        "@types/node": "^16",
        "@types/aws-lambda": "^8.10.110",
        "typescript": "^4.9.5"
    },
    "dependencies": {
        "@pulumi/pulumi": "^3.0.0",
        "@pulumi/aws": "^5.0.0",
        "@pulumi/awsx": "^1.0.0"
    }
}
